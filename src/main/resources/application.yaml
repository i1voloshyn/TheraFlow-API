spring:
  application:
    name: TheraFlow

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    locations: "classpath:db/migration"

theraflow:
  password-length:
    min-length: 10
    max-length: 64
